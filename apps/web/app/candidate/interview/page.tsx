// OWNER: Person 1 (Candidate Experience)
// Surface: web (simulator) — mirrors what the extension overlay shows in real calls
// Do not edit without coordinating in group chat.

// TODO(Person 1): mount QuestionDisplay + CaptionOverlay + PauseProcessButtons + ComfortCompanion
// TODO(Person 1): wire Web Speech API for live transcription
// TODO(Person 1): subscribe to interviewer state via shared-lib/supabase/realtime
// TODO(Person 1): respect Preferences (fontScale, captionsEnabled, comfortCompanionEnabled)

export default function CandidateInterviewPage() {
  return (
    <main>
      <h1>Candidate — simulated interview</h1>
    </main>
  );
}
